/**
 * App configuraion configuration
 */
module.exports.app = {
  /**
   * port of http server to listen.
   */
  port: 1337,

  /**
   * Default site name, usefull for rendered html files
   */
  site_name: 'Spine - holds your head straigth !',
};
